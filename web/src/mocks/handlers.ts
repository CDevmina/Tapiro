import { http, HttpResponse } from "msw";

// Define your API handlers here. Start with an empty array.
// Example:
// http.get('/api/users/profile', () => {
//   return HttpResponse.json({ id: '123', name: 'Test User' });
// }),
export const handlers = [];
